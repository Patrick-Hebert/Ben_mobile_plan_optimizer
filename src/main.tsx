import React from "react";
import ReactDOM from "react-dom/client";
import PlanOptimizerApp from "./app";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PlanOptimizerApp />
  </React.StrictMode>
);
